APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
ASSET_URL=

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# CMS Domain Routing
# ==================
#
# CMS_DOMAINS: Comma-separated list of domains where CMS is served at root (/)
# Example: live.tastymaldives.com,cms.tasty.mv
CMS_DOMAINS=
#
# CMS_ONLY: When true, CMS domains will ONLY serve CMS (no public website)
# When false (default), both CMS and website are accessible on CMS domains
CMS_ONLY=false
#
# CMS_DISABLE_PATH_ACCESS: When true, /cms path returns 404 on non-CMS domains
# Useful for production when CMS should only be accessed via dedicated domain
CMS_DISABLE_PATH_ACCESS=false
#
# Examples:
# ---------
# Local development (default): CMS at /cms, website at /
#   CMS_DOMAINS=
#   CMS_ONLY=false
#   CMS_DISABLE_PATH_ACCESS=false
#
# Staging (both accessible everywhere):
#   CMS_DOMAINS=staging.tastymaldives.com
#   CMS_ONLY=false
#   CMS_DISABLE_PATH_ACCESS=false
#
# Production (CMS on separate domain only):
#   CMS_DOMAINS=live.tastymaldives.com
#   CMS_ONLY=true
#   CMS_DISABLE_PATH_ACCESS=true

# DigitalOcean Spaces (for media, OG images & assets)
DO_SPACES_KEY=
DO_SPACES_SECRET=
DO_SPACES_REGION=sgp1
DO_SPACES_BUCKET=tasty
DO_SPACES_ENDPOINT=https://sgp1.digitaloceanspaces.com
DO_SPACES_CDN_URL=https://tasty.sgp1.digitaloceanspaces.com

# Media library disk and prefix (for environment separation)
# Prefix options: production, staging, develop
MEDIA_DISK=do
MEDIA_PREFIX=production

# Vite assets (set via GitHub Actions during build)
# Local dev doesn't need this - assets are served locally
# VITE_ASSET_URL=
